# Testing Guide

This document describes how to run and write tests for [Project Name].

## Running the test suite

```bash
# e.g. npm test / pytest / cargo test
```

## Test structure

Describe where tests live and how they're organized — e.g. unit tests
next to source files, integration tests in `tests/`, end-to-end tests in
`e2e/`.

## Writing a new test

Describe your conventions: naming, fixtures/mocks, how to add a test case
for a bug fix versus a new feature.

```bash
# Minimal example of a test file's shape
```

## Coverage expectations

State what's expected of a pull request — e.g. "new code should be
covered by tests," and whether coverage is enforced in CI.

## Linting and static analysis

```bash
# e.g. npm run lint / ruff check / cargo clippy
```

## Continuous integration

Describe what CI runs on each pull request and where to see results (link
to your CI provider's dashboard or workflow file).
